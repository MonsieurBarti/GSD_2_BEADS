---
name: gsd2b-help
description: List available GSD2B commands and usage
allowed-tools:
  - Bash
---

Show the available gsd2b commands by running:

```bash
node dist/loader.js help
```

Then explain each command briefly and mention that gsd2b uses `bd` (beads) for all issue tracking and state management.
